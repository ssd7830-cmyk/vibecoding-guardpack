# 비가역 안전 가이드 — MD, permissions, sandbox, hooks를 분리한다

> 용도: 사람용 설정 안내. 글로벌 CLAUDE.md에서 import하지 않는다. 제품 버전에 맞는
> 공식 문서를 확인하고 한 층씩 적용·시험한다.

## 1. 세 층의 역할

| 층 | 역할 | 증명할 수 없는 것 |
|---|---|---|
| CLAUDE.md | 판단과 보고 행동을 유도 | 반드시 차단된다는 보장 |
| permissions | 도구 호출을 allow/ask/deny | 허용된 임의 스크립트 내부 행동의 OS 격리 |
| sandbox | Bash와 자식 프로세스의 파일·네트워크 격리 | built-in 도구의 모든 권한 판단 |
| hooks | 정해진 lifecycle에서 검사·차단 handler를 실행 | prompt·agent handler의 판단 정확성, 잘못 작성된 hook의 안전 |

한 층의 문구나 구문 통과를 다른 층의 차단 증거로 쓰지 않는다.

## 2. 기존 설정부터 감사

적용 전에 활성 settings source와 기존 allow·ask·deny·additionalDirectories뿐 아니라
sandbox의 filesystem·network·credentials·excludedCommands, hooks와 permission mode를
해결된 최종값 기준으로 확인하고 백업한다. 특히 다음은 글로벌 allow에 두지 않는 쪽을 기본으로 한다.

- 임의 코드를 받을 수 있는 광범위한 interpreter·shell·package script
- 운영 배포·DB 변경·발송·외부 업로드
- 사용자 홈이나 Desktop 전체 같은 넓은 읽기·쓰기 경로
- 프로젝트에만 필요한 명령과 디렉터리

프로젝트 전용 권한은 해당 프로젝트의 .claude/settings.local.json 등 가장 좁은 scope로
옮긴다. 문자열 deny 하나만 믿지 않는다. 허용된 Python·Node·package script가 같은 파일
행동을 다른 방식으로 수행할 수 있기 때문이다.

## 3. 초보자용 보수적 도입 순서

1. Manual 또는 Plan 모드에서 현재 프로젝트의 경계와 필요한 명령을 확인한다.
2. /sandbox에서 프로젝트 단위 sandbox를 먼저 켜고 일반 빌드·테스트가 되는지 확인한다.
3. 사용자 전역으로 확대할 필요가 확인된 뒤에만 user settings를 변경한다.
4. 운영·배포·DB·발송은 프로젝트별 ask/deny와 작은 PreToolUse hook으로 별도 차단한다.
5. 각 단계마다 정상 작업 성공과 위험 fixture 차단을 둘 다 시험하고, 실패하면 그 단계만
   백업으로 되돌린다.

공식 문서의 현재 핵심 키는 sandbox.enabled, sandbox.failIfUnavailable,
sandbox.allowUnsandboxedCommands, sandbox.autoAllowBashIfSandboxed,
sandbox.filesystem.denyRead·allowRead, sandbox.credentials, sandbox.excludedCommands,
permissions.disableBypassPermissionsMode다. 마지막 키의 차단 값은 Boolean이 아니라
`"disable"`이다. 지원 버전과 OS를 확인하고 기억으로 JSON을 붙여 넣지 않는다.
failIfUnavailable과 우회 금지는 작업을 완전히 멈출 수 있으므로 먼저 프로젝트 fixture에서
검증한다.

기본 sandbox는 작업공간 밖 **쓰기**를 제한하지만 홈 전체 **읽기**를 자동 차단하지 않는다.
프로젝트 전용 읽기 경계를 원하면 denyRead와 allowRead를 함께 설계한다. 상대 경로는
project settings에서는 project root, user settings에서는 `~/.claude`를 기준으로 해석된다.
따라서 user settings의 `.`은 프로젝트 작업공간을 뜻하지 않는다. 자격증명 파일·환경변수는
credentials로 별도 열거한다.
excludedCommands는 allowUnsandboxedCommands가 false여도 sandbox 밖에서 실행되고 여러
scope의 배열이 합쳐지므로 전부 감사한다.

## 4. 실제 차단 시험

경고 문구가 아니라 임시 fixture에서 결과를 확인한다.

- 프로젝트 안의 허용된 일반 파일 쓰기와 테스트는 성공
- 설정한 읽기 경계 밖의 가짜 파일 읽기와 쓰기 경계 밖의 가짜 파일 쓰기는 실패
- Bash 직접 접근과 허용 Python·Node·package script를 통한 동일 접근도 실패
- built-in Read/Edit/Write는 별도의 permission rule대로 ask·deny
- excludedCommands와 unsandboxed retry가 의도한 목록 밖에서 실행되지 않음
- 미허용 네트워크는 실패 또는 명시적 승인
- 운영 모사 명령은 ask 또는 deny
- sandbox가 시작되지 않을 때 설정한 정책대로 실패하며 몰래 비격리 실행되지 않음
- command형 PreToolUse hook은 정상·차단 경로와 parser 오류를 시험한다. 차단 경로는 명시적
  exit 2 등 문서화된 block 신호를 내야 한다. malformed output·timeout은 hook 자체에서
  fail-closed가 아니라 no-decision으로 진행될 수 있으므로 permission ask/deny·sandbox 같은
  fallback도 함께 시험하고 hook timeout 하나를 안전 gate로 쓰지 않는다.

실제 운영 자격증명·DB·발송 대상을 시험에 쓰지 않는다.
도메인 allowlist 한 번의 성공·실패는 완전한 유출 차단 증명이 아니다. 기본 proxy는 TLS 내용을
검사하지 않으며 domain fronting 가능성이 문서화돼 있다. 강한 위협 모델은 TLS를 검사하는
별도 proxy·컨테이너·VM 등과 함께 검증한다.

## 5. 고위험 행동

운영 데이터 삭제·스키마 변경, 결제, 이메일·메시지 발송, 배포·공개 업로드, 민감정보
외부 전송, 복구본 없는 데이터 교체, 강제 push와 광범위 Git 원복은 실행 직전 정확한
대상·영향·외부 부작용·복구 가능성을 보여주고 승인받는다.

일반 파일의 가역 편집, 테스트, 로그·diff 확인까지 전부 승인 대상으로 만들면 승인 피로가
생긴다. 위험 동작을 좁게 정의하고 sandbox 안의 정상 작업 성공률, 승인창 수, deny 뒤 retry
횟수도 함께 기록한다.

## 6. Git과 체크포인트의 경계

- git init 전에 현재 경로, 상위 저장소, 포함 범위, 미추적 파일, .gitignore, 비밀정보를
  확인한다.
- commit·stash·reset·checkout·clean 전에 status, unstaged diff, staged diff, untracked와
  관련 ignored 파일을 확인해 사용자 변경과 이번 변경을 구분한다. checkout·clean이 일부
  파일만 대상으로 해도 복구 곤란 데이터가 사라질 수 있으면 정확한 대상과 백업을 먼저
  확인하고 승인받는다.
- Claude 체크포인트는 주 세션의 직접 편집 도구 변경 복구에 제한적으로 쓰고, 장기 이력은
  검토된 Git commit에 둔다. Bash로 바뀐 파일, 대부분의 subagent 편집, 다른 세션·사용자의
  동시 변경, symlink·hard-link 대상은 rewind로 복원되지 않을 수 있다.
- Git과 체크포인트는 DB·배포·결제·발송·외부 API·저장소 밖 상태를 복구하지 못한다.
  별도 백업·스냅샷·테스트 계정·롤백 절차가 필요하다.

## 7. 운영 설정 감사와 코드 보안 감사의 경계

두 감사는 서로 다른 질문에 답한다.

| 감사 | 보는 범위 | 결과가 증명하지 않는 것 |
|---|---|---|
| `guardpack-safety-audit` | CLAUDE.md, permissions, sandbox, hooks, Git·외부 작업 경계 | 애플리케이션 코드에 취약점이 없다는 것 |
| Claude Code `/security-review` | `origin/HEAD` 대비 현재 브랜치의 코드 diff에서 새 고신뢰 취약점 | 설정·권한이 안전하다는 것, 저장소 전체·working tree·배포 결과가 안전하다는 것 |

둘 다 필요하면 가드팩 감사를 먼저 실행해 Git과 누락 범위를 확인하고, 인계 상태가
`실행 가능`일 때만 사람이 별도 메시지에서 `/security-review`를 직접 호출한다. 자동 연쇄
호출하지 않는다. 한쪽의 미발견·실패·미실행을 다른 쪽의 통과 근거로 합치지 않는다.

현재 검증 기준인 Claude Code 2.1.220의 내장 감사는 `git diff origin/HEAD...`의 커밋 diff를
대상으로 한다. staged·unstaged·untracked 파일은 그 diff 본문에 포함되지 않는다. 제품 동작은
바뀔 수 있으므로 실제 버전의 공식 문서와 명령 지원 여부를 다시 확인한다.

가드팩 감사의 인계 상태는 다음처럼 닫힌 범위로 기록한다.

- `실행 가능`: Git worktree, 명령 지원, `origin/HEAD`·`HEAD`, 커밋 diff가 확인되고 누락 변경이 없음
- `검토 대상 없음`: 커밋 diff가 0이고 working tree도 깨끗함
- `범위 누락`: staged·unstaged·untracked 등 내장 감사에서 빠지는 변경이 있음
- `미실행`: 비 Git, `origin/HEAD` 없음, 권한·API 실패 등
- `미지원`: 현재 버전에 명령이 없음

비 Git 폴더나 기준 ref 누락을 고치기 위해 `git init`, commit, remote 추가·fetch·set-head를
자동 실행하지 않는다. 별도 변경 승인이 없다면 정확한 누락과 다음 판별법만 보고한다.
내장 감사가 발견을 보고하지 않아도 “검토 범위에서 보고 기준을 넘은 고신뢰 새 취약점이
보고되지 않음”까지만 말한다. `안전`, `통과`, `보안 완료`로 확대하지 않는다.

## 8. 의존성 설치

정확한 패키지명, 공식 문서가 가리키는 저장소·레지스트리, 발행자, 유지보수·보안 공지,
manifest·lockfile·패키지 매니저 일치, 설치 스크립트와 요청 권한을 확인한다. 다운로드 수나
등록일만으로 안전성을 판단하지 않는다.

공식 문서:
[Permissions](https://code.claude.com/docs/en/permissions) ·
[Sandboxing](https://code.claude.com/docs/en/sandboxing) ·
[Hooks](https://code.claude.com/docs/en/hooks-guide) ·
[Checkpointing](https://code.claude.com/docs/en/checkpointing) ·
[Commands](https://code.claude.com/docs/en/commands)
