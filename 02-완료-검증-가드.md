# 완료 검증 플레이북 — 주장과 증거를 1:1로 연결한다

> 용도: 중요한 기능, 조사 결과, 설정 적용처럼 완료 판정이 필요한 작업에서 읽는다.

## 1. 완료 조건

- 사용자가 관찰할 결과와 제외 범위를 기준으로 한다.
- 기존 테스트·점수·행 수가 proxy라면 실제 목적을 대변하는지 먼저 확인한다.
- 작업이 비자명하고 조건이 없으면 관찰 가능한 조건을 짧게 제안한다.
- 오타·단순 문구 수정에 별도 계약이나 긴 계획을 강제하지 않는다.

## 2. 주장별 증거

| 주장 | 알맞은 증거 예시 | 단독으로 부족한 것 |
|---|---|---|
| 코드가 바뀜 | 대상 diff | 동작 성공 주장 |
| 버그가 고쳐짐 | 원 재현 통과 + 관련 회귀 | 빌드만 성공 |
| UI가 맞음 | 실제 화면·상호작용·필요시 접근성 | 소스 diff만 확인 |
| API 연동이 됨 | 요청·응답·오류 경로·권한/버전 | 타입 검사만 통과 |
| 설정이 적용됨 | 구문 검사 + 실제 로드·적용 범위 | JSON이 파싱됨 |
| 문서·데이터가 맞음 | 원출처·계산·링크·형식·행/필드 | 생성 완료 메시지 |
| 안전장치가 막음 | 실제 차단 fixture | MD에 금지 문구 존재 |

검증 범위는 주장 범위와 같아야 한다. 사용자가 런타임 결과를 요구하지 않은 순수 문구
수정에 런타임을 억지로 요구하지 않고, 반대로 사용자 시나리오를 빌드 성공으로 축소하지 않는다.

## 3. 원래 실패와 주변 회귀

- 원래 실패 입력을 먼저 재현하고 수정 후 같은 입력으로 확인한다.
- 원인과 연결된 판별 검사와 영향을 받은 주변 계약을 적정 범위에서 확인한다.
- 입력 경계가 실제로 있는 기능에만 빈값·긴 값·한글·특수문자·권한 오류 같은 관련
  경계값을 검사한다.
- 시간·캐시·순서·동시성이 관찰과 연결되면 단발 통과를 재현 성공으로 과장하지 않는다.

## 4. 채점 기준 보호

- 통과만을 목적으로 기존 기대값을 약화하거나 skip·무조건 성공·특정 입력 하드코딩을
  사용하지 않는다.
- 요구사항 변경, 잘못된 테스트 수정, 새 회귀 테스트 추가는 정상일 수 있다. 바뀐 기준과
  이유, 사용자 결과에 미치는 영향을 설명한다.
- 자동 점수가 좋아졌는데 실제 사용자 결과가 나빠지면 proxy부터 재검토한다.

## 5. 별도 검토의 위치

같은 세션의 결정론적 검증을 먼저 수행한다. fresh reviewer는 다음 조건에서 추가한다.

- 남은 판정이 고영향·주관적이거나 장기 무인 작업이다.
- 새로운 정보원·검사·전문성이 실제 결론을 바꿀 수 있다.
- 추가 비용이 오판 비용보다 작다.

reviewer에게 “문제를 찾아라”가 아니라 목표·요구·허용 범위·판정기준을 주고, 정확성이나
요구사항에 영향을 주는 결함이 없으면 “없음”이라고 보고하게 한다.

## 6. 완료 보고

형식 칸을 채우는 것이 목적이 아니다. 실제 해당하는 내용만 다음 순서로 쓴다.

1. 바뀐 결과
2. 주장별 실행 증거
3. 미검증·우회·차단 사항과 영향

없던 작업을 “없음”으로 길게 나열하지 않는다. 실행하지 못한 항목을 완료로 포장하지 않는다.
