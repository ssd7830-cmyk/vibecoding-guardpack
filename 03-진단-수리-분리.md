# 진단·수리 플레이북 — 유관 경계와 판별 증거

> 용도: 원인이 불명확한 버그, 반복 실패, 영향이 큰 수정에서 읽는다. 오타처럼 원인과
> 수정이 직접 관찰되는 저위험 작업에는 이 절차를 강제하지 않는다.

## 1. 문제와 채점기부터 확인

1. 사용자가 관찰한 결과와 원하는 결과를 구분해 적는다.
2. 재현 조건, 환경·버전, 입력, 실제 출력을 기록한다.
3. “테스트 통과” 같은 proxy가 실제 사용자 결과를 대변하는지 확인한다.
4. 제외 범위와 위험 경계를 정한다.

증상을 재현했다고 원인을 확인한 것은 아니다. 오류 메시지는 어디서 실패가 관찰됐는지를
알려줄 뿐, 그 오류를 만든 최초 지점을 항상 알려주지는 않는다.

## 2. 넓되 유관한 시스템 경계

기대와 실제가 처음 갈라지는 지점을 찾는다. 아래 축은 체크리스트가 아니라 탐색 후보다.
관찰과 연결되어 원인과 전파를 구분하는 축만 본다.

- 생산자·호출/제어 흐름·데이터 변환·후속 소비자
- 메모리·DB·파일·캐시·세션 같은 상태
- 설정·환경변수·권한·빌드·런타임·버전 차이
- 네트워크·외부 API·큐·스토리지 같은 외부 경계
- 실행 순서·시간·만료·재시도·동시성
- 최근 변경과 사용자 기존 diff

함수 본문이 틀린 것처럼 보여도 입력 계약이나 후속 소비자가 다른 단위를 기대할 수 있다.
반대로 저장소 전체를 읽는 것도 넓게 보는 것이 아니다. 다음 경계를 확인해도 후보 순위나
행동이 달라지지 않으면 멈춘다.

## 3. 증거 ledger

중요한 항목만 다음 형식으로 남긴다.

| ID | 관찰과 provenance | 해석/후보 | 예측 | 판별 결과 |
|---|---|---|---|---|
| E1 | 명령·입력·출력·시각·파일/원출처 | 사실과 분리 | 후보가 맞을 때 달라질 것 | 지지/약화/배제/판별불가 |

- 같은 원 로그·테스트·문서를 여러 에이전트가 요약해도 E1 하나다.
- 해석과 후보는 관찰 칸에 쓰지 않는다.
- 새 관찰이 선택 가설과 모순되면 서사를 지키지 말고 상태를 갱신한다.
- 원 출력이 크면 비밀·개인정보를 가린 간결한 ledger와 접근 제한 파일의 경로·해시를 남기고
  전부 프롬프트에 복사하지 않는다. 보관 위치·접근 범위·기간도 필요한 만큼으로 제한한다.

## 4. 가설과 판별 probe

- 후보 수를 미리 정하지 않는다. 현재 관찰을 설명하고 다른 후보와 구별되는 결과를
  예측할 때만 후보로 둔다. 측정 오류와 “아직 모름”도 가능한 상태다.
- 모든 후보는 같은 사용자 목표·제약·평가기준으로 비교한다.
- probe는 가능한 결과에 따라 후보 순위나 다음 행동이 바뀌어야 한다. 바뀌지 않으면
  판별 검사가 아니다.
- 먼저 저비용·저교란·가역 probe를 고른다. read-only 질의, 최소 재현, 설정 비교는 수리
  patch와 분리해 표시한다. 로그 추가처럼 파일을 바꾸는 probe는 수정 권한이 있는 요청에서만
  쓰고, 보고 전용 요청에서는 read-only probe만 쓴다.

## 5. 수리 게이트

다음을 모두 만족하면 바로 최소 수리와 검증까지 진행한다.

- 결함이 직접 관찰됐다.
- 유관 경계가 수정 범위를 정할 만큼 이해됐다.
- 원인과 변경의 연결이 판별 결과로 뒷받침된다.
- 변경이 저위험·가역적이다.
- 원래 사용자 결과를 확인할 방법이 있다.

충족하지 않으면 수리처럼 보이는 추측성 patch를 쌓지 않는다. 안전한 probe까지만 수행하고
확인된 사실, 남은 후보, 다음 판별법, 예상 변경 범위를 보고한다.

## 6. 실패 뒤 갱신과 중단

- 각 repair patch의 대상·diff·명령·실제 출력·결과를 기록하고 한 번에 효과를 확인한다.
- 실패 patch는 이번 시도분만 분리해 제거한다. 사용자 기존 변경을 blanket
  reset·checkout·stash로 처리하지 않는다.
- 새 판별 정보 없이 같은 기전을 다시 고치거나, 패치가 겹쳐 원인을 분리할 수 없거나,
  다음 단계가 고위험·권한 밖이면 자동 수리를 멈춘다.
- 연속 실패 뒤 fresh session을 쓰더라도 목표·현재 diff·검증 결과·실패 증거·배제된
  후보를 간결히 넘긴다. 실패자의 미확정 해석은 결론이 아니라 후보로 표시한다.
- 새 인과 증거가 생기면 과거 횟수만으로 다음 수리를 금지하지 말고 새 근거·위험·검증
  가능성으로 다시 판단한다.

## 7. 성공 판정

원래 사용자 시나리오, 원인과 연결된 판별 검사, 영향을 받은 회귀가 주장 범위에 맞게
통과해야 한다. 우회 경로만 동작하거나 재현이 불안정하면 그 상태를 그대로 보고한다.
